{
	"name": "Trigger_7h",
	"properties": {
		"annotations": [],
		"runtimeState": "Started",
		"pipelines": [
			{
				"pipelineReference": {
					"referenceName": "CopyPipeline_Full",
					"type": "PipelineReference"
				},
				"parameters": {
					"windowStart": {
						"type": "Expression",
						"value": "@trigger().scheduledTime"
					},
					"directoryTime": "{{\"type\":\"Expression\",\"value\":\"@{if(Equals(addHours(convertTimeZone(trigger().scheduledTime, 'UTC', 'Central Standard Time'), +7, 'HH'),addHours(trigger().scheduledTime, +2 'HH')),addhours(trigger().scheduledTime, +2),addhours(trigger().scheduledTime, +1))}\"}"
				}
			}
		],
		"type": "ScheduleTrigger",
		"typeProperties": {
			"recurrence": {
				"frequency": "Hour",
				"interval": 7,
				"startTime": "2022-04-28T10:00:00",
				"timeZone": "W. Europe Standard Time"
			}
		}
	}
}